可以，但我不建议只靠提示词。

对你这个仓库，更稳的方案是：

**Codex 提示词负责：检查、分析、改代码、处理冲突。
Git/GitHub Actions 负责：同步上游、构建、测试、lint、确保不会把项目合坏。**

我看了你的仓库情况：`letmedothis/Legado_Max` 是 fork，当前默认分支是 `main`，你自己的开发分支是 `my-changes`；直接 parent 是 `Suml-1/Legado_Max`。你的 `CLAUDE.md` 里已经规定了不少项目规则，例如 JDK 17、minSdk 23 / compileSdk 37 / targetSdk 37、主要 flavor 是 `appMax`、需要跑 Gradle build/test/lint，而且明确规定 AI 不允许擅自执行 `git push`、`git rebase`、`git reset --hard` 等危险操作。

所以我建议你把工作流设计成下面这样。

------

## 一、代码扫描不要用一句「帮我找 Bug」

你需要的是一套针对 **Legado_Max** 的项目级扫描提示词。

可以直接把下面这套交给 Codex。

你现在是本项目的高级 Android/Kotlin 代码审查工程师。

目标不是进行普通的代码风格检查，而是系统性查找：

1. 会导致程序无法编译、无法启动、运行异常的 Bug
2. 会导致 Crash / ANR / 卡死 / 死循环的 Bug
3. 会导致数据损坏、数据库异常、状态错乱的 Bug
4. 会导致明显性能下降的代码
5. 会导致内存泄漏、线程泄漏、协程泄漏的代码
6. 会导致网络请求异常、资源耗尽或重复请求的代码
7. 会导致阅读体验明显下降的 Bug
8. Android API 23～37 兼容问题
9. Release / ProGuard / R8 环境下才出现的问题
10. 最近修改代码引入的回归问题

这是一个 Android 阅读器项目 Legado_Max。

开始检查前：

- 先完整阅读项目根目录 CLAUDE.md
- 阅读 docs/project-rules/README.md
- 根据涉及模块读取对应 project-rules
- 不要仅根据文档判断代码行为，源码优先
- 如果文档和源码不一致，以源码为准并指出文档可能已经过时

项目关键环境：

- Kotlin
- Android
- MVVM
- AndroidViewModel
- ViewBinding
- Jetpack Compose + XML View 混合
- Room
- Coroutines
- Rhino JavaScript Engine
- OkHttp / Cronet
- WebSocket / NanoHTTPD
- minSdk 23
- targetSdk 37
- compileSdk 37
- JDK 17
- 主开发 flavor：appMax

重点分析以下区域：

### 1. Crash 风险

查找：

- NullPointerException
- IndexOutOfBoundsException
- ClassCastException
- IllegalStateException
- ConcurrentModificationException
- NumberFormatException
- JSON 解析异常
- Room 查询返回值假设错误
- lateinit 未初始化
- !! 滥用
- Activity / Fragment 生命周期异常
- Context 使用错误
- Intent extra 缺失
- Bundle 参数缺失
- API level 不兼容

不要仅搜索关键字，要结合调用链分析实际是否可能发生。

### 2. 协程与线程问题

重点分析：

- Main Thread 上是否执行 IO
- Dispatchers.IO 是否被错误切回 Main
- BaseViewModel.execute() 使用方式是否违反项目协程规范
- CoroutineScope 生命周期是否正确
- Activity / Fragment 销毁后任务是否仍然运行
- Job 是否可能泄漏
- Flow 是否重复 collect
- collectLatest / collect 使用是否错误
- suspend 函数是否存在阻塞调用
- runBlocking 是否出现在运行路径
- Mutex / synchronized 是否可能死锁
- Channel 是否可能阻塞
- 是否存在无限循环或高频轮询

特别检查：

app/src/main/java/io/legado/app/help/coroutine/

以及所有：

execute {
}

调用链。

### 3. 性能问题

寻找实际会影响用户体验的问题：

- 主线程磁盘 IO
- 主线程数据库访问
- 主线程网络访问
- 大量 JSON parse
- 大量字符串拼接
- Regex 重复编译
- 高频对象创建
- 大 Bitmap
- Bitmap 未释放
- 图片重复解码
- RecyclerView 重复刷新
- notifyDataSetChanged 过度使用
- Compose 无意义 recomposition
- LazyColumn key 错误
- State 使用错误
- 重复网络请求
- 重复数据库查询
- N+1 查询
- 大表全表扫描
- 无索引查询
- 大文件一次性读入内存
- EPUB/TXT 解析内存峰值
- JS/Rhino 执行阻塞
- WebView/HTTP 请求资源泄漏

不要把微不足道的优化列出来。

只报告：
会对真实用户产生可观察影响的问题。

### 4. 内存泄漏

重点分析：

- Activity Context 被 Singleton 持有
- View 被长生命周期对象持有
- Handler / Runnable 泄漏
- CoroutineScope 泄漏
- callback 未移除
- LiveData observer 未释放
- EventBus observer 未释放
- Bitmap cache
- WebView
- InputStream
- OutputStream
- Cursor
- ResponseBody
- Socket
- WebSocket
- Executor
- Thread

重点检查：

ReadBook
CacheBook
AudioPlay

以及 model/ 中所有 Singleton/Object。

### 5. Room 数据库

检查：

- migration
- schema version
- destructive migration
- transaction
- 并发写入
- DAO 返回值
- nullable
- delete/update 范围错误
- 主键冲突
- 数据覆盖
- N+1 query
- 无索引查询
- 大量循环 DAO 调用

重点防止：

数据丢失
书架数据损坏
阅读进度错误
书源数据损坏

### 6. 阅读核心流程

重点检查：

书籍打开
目录加载
章节加载
正文解析
翻页
阅读进度
缓存
书签
高亮
搜索
字体/主题
TXT
EPUB

检查是否存在：

状态竞争
章节错位
页码错位
缓存错误
线程竞争
数据覆盖
重复请求

### 7. 网络层

重点检查：

OkHttp
Cronet
WebBook
WebSocket
NanoHTTPD

检查：

- Response 是否关闭
- connection 泄漏
- timeout
- retry 无限循环
- redirect
- Cookie
- gzip
- encoding
- Range
- DNS
- proxy
- SSL
- HTTP error handling
- coroutine cancel 后网络请求是否取消

### 8. Rhino / JS 规则引擎

重点分析：

modules/rhino
model/analyzeRule

检查：

- JS 无限循环
- 超长 JS 执行
- Context 泄漏
- ThreadLocal
- Rhino Context enter/exit
- JS exception
- JS 与 Kotlin/Java 交互类型转换
- 大量规则执行造成 CPU 占用
- 用户规则导致整个 App 卡死

### 9. Android API 23～37

重点查找：

- 高版本 API 未做 SDK 判断
- Android 12+
- Android 13+
- Android 14+
- Android 15+
- Android 16+
- targetSdk 37 行为变化
- PendingIntent flags
- foreground service
- notification permission
- storage
- SAF
- exported component
- edge-to-edge
- WebView
- 16KB page size
- native libraries

不要仅根据 IDE warning 判断。

结合项目 minSdk 23 判断真实兼容问题。

### 10. Release-only Bug

检查：

R8
ProGuard
reflection
serialization
Room
Rhino
JNI
Cronet

确认 Debug 正常但 Release 是否可能失败。

### 11. Git 最近改动

执行并分析：

git status
git log --oneline --decorate -30

然后：

git diff main...my-changes

优先检查 my-changes 相对于 main 修改过的代码。

因为这些代码是最容易产生回归 Bug 的地方。

同时检查这些修改是否破坏原有调用者。

### 12. 上游更新兼容性

如果存在 upstream remote：

git fetch upstream

比较：

git diff HEAD...upstream/main

分析：

- 上游是否修改了我也修改过的文件
- 是否可能产生 merge conflict
- 即使 Git 不冲突，是否存在语义冲突
- API 是否发生变化
- 方法签名是否变化
- 数据结构是否变化
- Gradle/依赖是否变化

扫描时不要立即修改代码。

第一阶段只进行分析。

输出：

# P0 致命问题

可能导致：

- 编译失败
- 启动失败
- 数据损坏
- 高频 Crash
- ANR

每个问题必须给：

文件
函数
相关代码
触发条件
调用链
实际影响
为什么确认这是问题
修复建议
置信度

# P1 严重问题

包括：

明显性能问题
内存泄漏
并发问题
网络资源泄漏
阅读流程错误

# P2 一般问题

真实存在但影响较小。

禁止为了增加数量报告：

代码风格
变量命名
注释
理论上的微优化
没有实际触发路径的问题。

完成分析后，再做一次自我复核：

对于每一个问题回答：

1. 是否存在真实调用路径？
2. 是否可能被已有代码保护？
3. 是否是 Android/库本身允许的行为？
4. 是否属于误报？
5. 是否可以构造触发场景？

无法证明的问题降级为：

「疑似问题，需要验证」

最后输出：

扫描文件数量
重点分析模块
P0 数量
P1 数量
P2 数量
疑似问题数量

并给出：

最值得优先修复的 Top 10 问题。

在我明确要求“开始修复”之前，不要修改任何源码。
不要执行 git push。
不要执行 git rebase。
不要执行 git reset --hard。

这一版比「扫描整个项目有没有 Bug」有效很多，因为它明确告诉 Codex **哪些问题值得找、哪些不要报、必须验证调用路径**。

------

# 二、修 Bug 用第二套 Prompt

扫描完成后不要让 Codex“一口气把全部 Bug 修掉”。

最好按 P0 → P1 分批。

例如：

根据刚才的扫描结果开始修复。

只修复：

P0 和高置信度 P1。

原则：

1. 优先最小修改
2. 不做无关重构
3. 不改变现有产品行为，除非该行为本身就是 Bug
4. 不随意改变数据库 schema
5. 不随意新增依赖
6. 不改变公共 API，除非确实必要
7. 遵守 CLAUDE.md 和 docs/project-rules/
8. 修改协程代码前读取 coroutine-rules.md
9. 修改 Repository 前读取 repository-rules.md
10. 修改 API 兼容代码前读取 api-compat-rules.md

每修复一个问题：

先说明：

问题原因
修改方案
影响范围

然后修改代码。

修改完成后执行：

./gradlew assembleDebug
./gradlew test
./gradlew lint

如果涉及 appMax：

优先验证：

./gradlew assembleAppMaxDebug

如果涉及 Release/R8：

追加：

./gradlew assembleAppMaxRelease

如果涉及 modules/web：

cd modules/web
pnpm install
pnpm build
pnpm lint

如果测试失败：

不要为了让测试通过而删除测试、降低检查级别或屏蔽异常。

找出实际失败原因并修复。

全部完成后执行：

git diff --check
git status
git diff

最后重新审查所有修改，重点确认：

- 是否引入新的 Null 问题
- 是否引入线程问题
- 是否产生行为变化
- 是否影响 API 23
- 是否影响 Release
- 是否产生重复请求
- 是否产生生命周期问题

最后给出：

修复问题列表
修改文件
测试结果
仍存在的问题
没有修复的问题及原因

不要执行 git push。
不要 rebase。
不要 reset --hard。
不要修改与本任务无关的代码。

------

# 三、上游同步不要直接 `git pull`

这里我特别建议你换一个比较稳的结构：

```
Suml-1/Legado_Max
        │
        │ upstream/main
        ▼
你的 fork
letmedothis/Legado_Max
        │
        ├── main
        │
        └── my-changes   ← 你的实际开发
```

Git 官方推荐的本质也是：

```
git fetch
git merge
```

`git pull` 实际就是 fetch + merge。[GitHub Docs](https://docs.github.com/en/get-started/using-git/getting-changes-from-a-remote-repository?utm_source=chatgpt.com)

你的场景最好不要直接：

```
git pull upstream main
```

而是拆开。

第一次配置：

```
git remote -v
```

如果没有 upstream：

```
git remote add upstream https://github.com/Suml-1/Legado_Max.git
```

然后：

```
git fetch upstream
```

确认：

```
git remote -v
```

应该类似：

```
origin    https://github.com/letmedothis/Legado_Max.git
upstream  https://github.com/Suml-1/Legado_Max.git
```

------

# 四、以后同步上游，我建议这样

你人在：

```
my-changes
```

先确认工作区：

```
git status
```

一定要没有未提交修改。

然后：

```
git fetch upstream
```

看上游新增了什么：

```
git log --oneline HEAD..upstream/main
```

再查看影响：

```
git diff --stat HEAD...upstream/main
```

然后：

```
git merge upstream/main
```

### 没冲突

Git 自动完成。

然后：

```
./gradlew assembleAppMaxDebug
./gradlew test
./gradlew lint
```

确认 OK 后：

```
git status
```

最后由你决定：

```
git push origin my-changes
```

------

# 五、有冲突，可以让 Codex 自动处理吗？

**可以。**

这恰恰很适合 Codex。

但不是简单地说：

> 帮我解决冲突。

而应该使用下面这套。

我要把 upstream/main 的最新代码合并到当前 my-changes 分支。

目标：

最大程度保留 upstream 的最新改动，同时保留 my-changes 中我主动实现的功能。

首先执行检查：

git status
git branch --show-current
git remote -v

必须确认：

当前分支为 my-changes。

如果工作区存在未提交修改：

停止 merge，并告诉我有哪些修改。

不要 stash。
不要 reset。
不要删除修改。

然后：

git fetch upstream

分析：

git log --oneline HEAD..upstream/main
git diff --stat HEAD...upstream/main
git diff HEAD...upstream/main

先总结：

1. upstream 更新内容
2. 涉及模块
3. 与 my-changes 是否修改相同文件
4. 潜在冲突位置
5. 潜在语义冲突

然后执行：

git merge upstream/main

如果没有冲突：

进入构建验证。

如果存在冲突：

逐个分析冲突文件。

处理冲突原则：

优先理解：

BASE
OURS = my-changes
THEIRS = upstream/main

禁止简单采用：

git checkout --ours
git checkout --theirs

除非确认整个文件只能选择其中一方。

对于每个冲突：

比较：

git diff
git log -p -- <file>

理解双方为什么修改该代码。

处理优先级：

1. 保留 upstream 的 Bug 修复
2. 保留 upstream 的 API / 数据结构变化
3. 保留 upstream 的安全修复
4. 保留 upstream 的性能优化
5. 同时重新适配 my-changes 的自定义功能
6. 避免把 upstream 已删除的旧实现重新引入
7. 避免重复实现同一逻辑

注意：

Git 没有产生冲突不代表没有问题。

合并完成后必须做一次“语义冲突扫描”。

重点查：

- 方法签名变化
- 类移动
- 参数变化
- nullable 变化
- Room schema
- migration
- Gradle
- dependency
- Compose API
- coroutine
- WebBook
- analyzeRule
- ReadBook
- CacheBook
- AudioPlay
- WebSocket
- Rhino

所有冲突解决后：

git diff --check
git status

然后运行：

./gradlew assembleAppMaxDebug
./gradlew test
./gradlew lint

涉及 Release / ProGuard / native 时：

./gradlew assembleAppMaxRelease

如果 modules/web 被 upstream 修改：

cd modules/web
pnpm install
pnpm build
pnpm lint

测试失败：

分析真实原因并修复 merge 导致的问题。

不要：

- 删除失败测试
- 忽略编译错误
- 使用 @Suppress 隐藏问题
- 为通过构建随意回退 upstream 修改

最终重新检查：

git diff upstream/main...HEAD

确认：

my-changes 的自定义功能仍然存在。

最后输出：

上游新增 commit
修改文件
自动合并文件
发生冲突文件
每个冲突采用的解决方案
构建结果
测试结果
lint 结果
仍需人工验证内容

允许执行 git merge。

禁止自动执行：

git push
git rebase
git reset --hard
git clean -fd
git commit --amend

完成后停下来，由我决定是否 push。

------

# 六、这里有一个很重要的问题：Git 冲突 ≠ 真正的冲突

比如上游原来：

```
fun loadBook(id: Long)
```

你写了：

```
loadBook(bookId)
```

后来上游改成：

```
fun loadBook(id: Long, forceRefresh: Boolean)
```

Git 有可能：

```
完全没有 CONFLICT
```

但是你的代码已经逻辑不正确甚至编译失败。

所以你的同步流程必须是：

```
fetch
 ↓
diff 分析
 ↓
merge
 ↓
解决文本冲突
 ↓
语义冲突扫描
 ↓
编译
 ↓
test
 ↓
lint
 ↓
检查 my-changes 功能有没有被上游破坏
```

不能只是：

```
merge → 没红字 → 完事
```

------

# 七、可以进一步做到“一条命令”

你以后甚至可以专门做个：

```
./sync-upstream.sh
```

大概负责：

```
git status
git fetch upstream
git merge upstream/main

./gradlew assembleAppMaxDebug
./gradlew test
./gradlew lint
```

但**冲突处理不要写死在 Shell 里**，交给 Codex 判断比较合适。

------

# 八、最终我推荐你采用这个结构

你的 Codex 工作流以后固定成四个命令思维：

```
/code-review
     ↓
全项目 Bug / 性能扫描

/fix
     ↓
修复 P0/P1

/sync-upstream
     ↓
upstream/main → my-changes
解决冲突 + 语义冲突

/verify
     ↓
build + test + lint + release 检查
```

甚至可以直接在项目里建立：

```
.codex/
├── review.md
├── fix.md
├── sync-upstream.md
└── verify.md
```

这样你根本不需要每次复制几千字 Prompt。

------

## 所以回答你「只靠提示词可以吗」

**代码检查：可以主要靠 Prompt。**

**自动同步 upstream：不能只靠 Prompt。**

最稳的是：

```
Prompt
+
Git
+
Gradle
+
Lint
+
Tests
+
GitHub Actions
```

Codex 的作用是当“工程师”，Git/Gradle/CI 是它的“安全带”。

而且你这个项目已经有比较完善的 `CLAUDE.md` 和 `docs/project-rules`，非常适合再加一层 **Codex 固定工作流**。你的仓库规则甚至已经明确要求 AI 对 `push/rebase/reset` 保守处理，所以完全可以把“检查 + 修复 + 同步上游”规范化。

如果让我给你配置，我会进一步给这个项目直接做成一套 **`.codex/` 目录 + `AGENTS.md` + 4 套固定 Prompt + `sync-upstream.sh` + GitHub Actions 自动检测 upstream 更新**。这样以后你在 Codex 里基本只需要说：

```
执行 upstream 同步流程
```

或者：

```
执行全项目 P0/P1 扫描
```

就行。